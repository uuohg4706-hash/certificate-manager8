# نظام إدارة الشهادات الرقمية 🎓

نظام متكامل لإدارة شهادات المدارس برمجياً مع دعم كامل للعربية والإنجليزية.

## المميزات 🌟

✅ **مصادقة آمنة** - تسجيل دخول برمز PIN للمعلمين
✅ **لوحة تحكم** - إدارة الشهادات (إضافة/حذف/بحث)
✅ **محرر متقدم** - تحرير الشهادات مع معاينة فورية
✅ **بوابة أولياء أمور** - البحث المفتوح عن الشهادات برؤية النتائج والتبارك
✅ **دعم لغات** - العربية والإنجليزية مع RTL/LTR
✅ **حفظ صور** - تحويل الشهادات إلى صور PNG
✅ **قاعدة بيانات** - تخزين البيانات على السيرفر
✅ **عمل بلا انترنت** - localStorage للعمل بدون اتصال

## البنية 📁

```
certificate-manager/
├── index.html           # الصفحة الرئيسية (بوابة الدخول)
├── login.html          # صفحة تسجيل الدخول للمعلمين
├── admin.html          # لوحة التحكم للمعلمين
├── editor.html         # محرر الشهادات
├── parents.html        # بوابة أولياء الأمور (بحث مفتوح)
├── server.js           # السيرفر Express.js
├── package.json        # المكتبات المطلوبة
├── certificates-data/  # مجلد تخزين الشهادات (ينشأ تلقائياً)
└── README.md          # هذا الملف
```

## متطلبات التثبيت 📦

- Node.js 14+ 
- npm أو yarn

## التثبيت المحلي 🚀

### 1. تثبيت المكتبات

```bash
npm install
```

### 2. تشغيل السيرفر

```bash
npm start
```

أو للتطوير مع إعادة تحميل تلقائي:

```bash
npm run dev
```

### 3. فتح التطبيق

افتح متصفحك على: 
```
http://localhost:3000
```

## الاستخدام 📝

### 1. تسجيل الدخول 🔐

- الرقم السري الافتراضي: `1234`
- يمكن تغييره في ملف `login.html` (السطر ~350)

### 2. لوحة التحكم

- عرض جميع الشهادات المحفوظة
- البحث حسب (الاسم/الفئة/المركز)
- إضافة شهادة جديدة
- حذف شهادة
- الدخول لتحرير الشهادة

### 3. محرر الشهادات

**الألقاب:**
- تعديل بيانات الطالب (الاسم/الفئة/المركز)

**الدرجات:**
- 7 مواد (عربي/رياضيات/الدراسات الاجتماعية/العلوم/اللغة الإنجليزية/الحاسوب/التربية البدنية)
- الحد الأدنى والأقصى لكل مادة: 0-50
- حساب تلقائي للمجموع والمتوسط والتقدير

**الغياب والحضور:**
- عدد أيام الحضور
- عدد أيام الغياب

**التوقيع:**
- اسم المدير أو المسؤول

**الخيارات:**
- معاينة فورية للشهادة
- تحويل اللغة (العربية/الإنجليزية)
- حفظ الشهادة + الصورة
- تنزيل الصورة
- تصدير PDF
- تصدير صورة PNG

### 4. بوابة أولياء الأمور 👨‍👩‍👧

- **رابط مباشر**: http://localhost:3000/parents.html
- **بحث مفتوح**: لا يحتاج تسجيل دخول
- **البحث**: أدخل اسم الطالب الكامل أو جزء منه
- **النتائج تعرض**:
  - 🎉 رسالة تبارك وتهنئة للطالب
  - 📋 البيانات الأساسية (الاسم/الفئة/المركز)
  - 📊 جدول الدرجات كامل (فصل أول + ثاني + المجموع)
  - 📅 الحضور والغياب
  - 📈 التقدير العام
  - 🖼️ صورة الشهادة إن وجدت
  - 🖨️ خيار الطباعة والتحميل
- تصدير صورة PNG

## API Endpoints 🔗

### حفظ شهادة جديدة
```
POST /api/certificates/save
Content-Type: application/json

{
  "studentName": "أحمد محمد",
  "studentCategory": "السنة الخامسة",
  "studentCenter": "مدرسة النيل",
  "certification": { /* بيانات الشهادة */ },
  "image": "data:image/png;base64,..."
}
```

### الحصول على جميع الشهادات
```
GET /api/certificates/list
```

### الحصول على شهادة محددة
```
GET /api/certificates/:id
```

### تنزيل صورة الشهادة
```
GET /api/certificates/image/:fileName
```

### حذف شهادة
```
DELETE /api/certificates/:id
```

## النشر على السحابة ☁️

### Vercel (موصى به للبدء السريع)

1. انسخ المشروع على GitHub
2. اذهب إلى https://vercel.com
3. اختر "Import Project"
4. اختر مشروعك
5. اضغط Deploy
6. ستجد رابط الواجهة الأمامية تلقائياً

```
https://your-project.vercel.app
```

**ملاحظة:** الملفات ستُحفظ في `/tmp` على Vercel (مؤقتة أثناء جلسة التشغيل).

### Render (موصى به للبيانات الدائمة)

1. انسخ على GitHub
2. اذهب إلى https://render.com
3. اختر "New +" ثم "Web Service"
4. اختر مشروعك
5. الإعدادات:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Port**: 3000

### Heroku (خيار تقليدي)

```bash
# تثبيت Heroku CLI
npm install -g heroku

# تسجيل الدخول
heroku login

# إنشاء تطبيق
heroku create your-app-name

# نشر المشروع
git push heroku main
```

### تشغيل محلي (VPS)

1. قم بتثبيت Node.js على الخادم
2. انسخ المشروع
3. شغل: `npm install && npm start`
4. استخدم Nginx كـ Reverse Proxy:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## تخزين البيانات 💾

### المحلي (localhost)

- **الموقع**: `certificates-data/` 
- **الصيغة**: JSON + PNG
- **الملفات**: `{timestamp}_{studentName}.json` و `{timestamp}_{studentName}.png`

### السحابة (Vercel)

- **المشكلة**: النظام الملفات مؤقت
- **الحل**: استخدام قاعدة بيانات خارجية (ستأتي لاحقاً)

### السحابة (Render/Heroku)

- **الموقع**: `/tmp` أو مجلد دائم
- **الاعتبارات**: قد تُمسح الملفات عند restart

## الأمان 🔒

⚠️ **تحذير:** هذا النظام استخدام تعليمي/داخلي فقط.

⚠️ **ملاحظة حول الخصوصية:**
- **بوابة أولياء الأمور** (parents.html) بحث مفتوح **بدون تشفير**
- جميع الشهادات المحفوظة يمكن الوصول إليها برؤية الاسم فقط
- **في الإنتاج**: يجب حماية بوابة أولياء الأمور بـ:
  - رقم هوية الطالب + تاريخ الميلاد
  - كود تحقق فريد لكل طالب
  - تشفير البيانات
  - HTTPS إجباري

للاستخدام الإنتاجي:
- [ ] استبدل PIN البسيط بـ JWT Tokens
- [ ] أضف HTTPS/SSL
- [ ] استخدم قاعدة بيانات (MongoDB/PostgreSQL)
- [ ] أضف صلاحيات مستخدمين
- [ ] حماية بوابة أولياء الأمور برموز توثيق
- [ ] ستر البيانات الحساسة
- [ ] إضافة Logging و Monitoring

## المتطلبات المستقبلية 🗓️

- [ ] قاعدة بيانات MongoDB
- [ ] صور ملفات دائمة (AWS S3/Cloudinary)
- [ ] تصدير دفعات من الشهادات
- [ ] نموذج طباعة مخصص
- [ ] إحصائيات وتقارير
- [ ] إشعارات بريدية
- [ ] تطبيق موبايل

## استكشاف الأخطاء 🐛

### "السيرفر غير متاح"

**الحل:**
```bash
# تأكد من تشغيل السيرفر
npm start

# تحقق من المنفذ 3000
lsof -i :3000  # على Mac/Linux
netstat -ano | findstr :3000  # على Windows
```

### خطأ في الحفظ

**الحل:**
1. افتح Developer Tools (F12)
2. انظر إلى Console للأخطاء
3. تأكد من أن `certificates-data/` قابل للكتابة

### الصور لا تظهر

**الحل:**
- ستأتي في النسخة التالية من السيرفر
- حالياً يمكنك تنزيل من `localStorage` محلياً

## الترخيص 📜

MIT

## الدعم 💬

للمساعدة أو الإبلاغ عن خطأ:
- تحقق من Console (F12)
- راجع README
- افتح Issue في GitHub

---

**الإصدار**: 1.0.0  
**آخر تحديث**: 2024
